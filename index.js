require('./src/conf/app.scss');
// var context = require.context('./src/pages/scss/', true, /\.(scss)$/);
// var files={};

// context.keys().forEach((filename)=>{
// //   console.log(filename);
//   files[filename] = context(filename);
// });